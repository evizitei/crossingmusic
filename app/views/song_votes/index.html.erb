<h1>Listing song_votes</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @song_votes.each do |song_vote| %>
  <tr>
    <td><%= link_to 'Show', song_vote %></td>
    <td><%= link_to 'Edit', edit_song_vote_path(song_vote) %></td>
    <td><%= link_to 'Destroy', song_vote, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Song vote', new_song_vote_path %>
