<%= form_for(@song_vote) do |f| %>
  <% if @song_vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song_vote.errors.count, "error") %> prohibited this song_vote from being saved:</h2>

      <ul>
      <% @song_vote.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
